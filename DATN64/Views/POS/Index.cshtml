@model List<DATN64.Models.SanPham>
@{ Layout = "~/Views/Shared/_Layout.cshtml"; ViewData["Title"] = "Bán hàng t?i qu?y (POS)"; }
<div class="container my-5">
    <h2>Bán hàng t?i qu?y (POS)</h2>
    <form asp-action="CreateOrderPOS" method="post">
        <div class="row">
            <div class="col-md-8">
                <h4>Danh sách s?n ph?m</h4>
                <table class="table">
                    <thead><tr><th>Mã SP</th><th>Tên SP</th><th>Giá Bán</th><th>T?n</th><th>Ch?n mua</th></tr></thead>
                    <tbody>
                        @foreach(var item in Model) {
                            <tr>
                                <td>@item.MaSanPham</td>
                                <td>@item.TenSanPham</td>
                                <td>@item.GiaBan.ToString("N0") d</td>
                                <td>@item.SoLuongTon</td>
                                <td>
                                    <input type="checkbox" name="productIds" value="@item.MaSanPham" />
                                    <input type="number" name="quantities" value="1" min="1" max="@item.SoLuongTon" style="width:60px"/>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
            <div class="col-md-4">
                <h4>Thông tin khách hàng</h4>
                <input type="text" name="customerPhone" class="form-control mb-2" placeholder="SÐT Khách hàng" />
                <input type="text" name="customerName" class="form-control mb-2" placeholder="Tên Khách hàng" />
                <select name="paymentMethod" class="form-control mb-2">
                    <option value="Ti?n m?t">Ti?n m?t</option>
                    <option value="Chuy?n kho?n">Chuy?n kho?n</option>
                </select>
                <button type="submit" class="btn btn-success w-100">Thanh Toán</button>
            </div>
        </div>
    </form>
</div>
