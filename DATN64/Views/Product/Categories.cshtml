@model List<DATN64.Models.DanhMuc>
@{ Layout = "~/Views/Shared/_Layout.cshtml"; ViewData["Title"] = "Qu?n lý danh m?c"; }
<div class="container my-5">
    <h2>Qu?n lý danh m?c</h2>
    <table class="table">
        <thead><tr><th>Mã DM</th><th>Tên DM</th><th>Mô T?</th></tr></thead>
        <tbody>
            @foreach(var item in Model) {
                <tr><td>@item.MaDanhMuc</td><td>@item.TenDanhMuc</td><td>@item.MoTa</td></tr>
            }
        </tbody>
    </table>
</div>
